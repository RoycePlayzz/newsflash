# NewsFlash frontend

React 19 application built with Vite 8.

## Scripts

- `npm run dev` — local development server on port 5173
- `npm run build` — production build
- `npm run preview` — preview the production build locally

The API base URL is configured with `VITE_API_URL` and defaults to `http://127.0.0.1:8000`.
